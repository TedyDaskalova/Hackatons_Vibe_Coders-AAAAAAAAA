@model AdminTicketsViewModel
@{
    ViewData["Title"] = "Билети";
}

<section class="groove-app-page">
    <div class="groove-page-hero">
        <div class="groove-page-hero__copy">
            <span class="groove-stamp groove-stamp-teal">Продажби</span>
            <h1>Обзор на <span>билетите</span>.</h1>
            <p>Следи активните типове билети, продажбите, използваните входове и демонстрационния приход.</p>
        </div>
        <div class="groove-page-actions">
            <a asp-action="Index" class="groove-button groove-button-paper">
                <i class="bi bi-arrow-left"></i> Към админ
            </a>
        </div>
    </div>

    <div class="groove-stat-grid">
        <article class="groove-stat-card">
            <span>Типове билети</span>
            <strong>@Model.TicketTypesCount</strong>
        </article>
        <article class="groove-stat-card">
            <span>Продадени</span>
            <strong>@Model.TicketsSoldCount</strong>
        </article>
        <article class="groove-stat-card">
            <span>Използвани</span>
            <strong>@Model.UsedTicketsCount</strong>
        </article>
        <article class="groove-stat-card">
            <span>Оставащи</span>
            <strong>@Model.UnusedTicketsCount</strong>
        </article>
        <article class="groove-stat-card">
            <span>Приход</span>
            <strong>@Model.TotalRevenue.ToString("0.00")</strong>
        </article>
    </div>

    <section class="groove-page-section">
        <div class="groove-section-bar">
            <div>
                <span class="groove-kicker">Последни покупки</span>
                <h2>Скорошно <span>движение</span>.</h2>
            </div>
        </div>

        @if (Model.Recent.Count == 0)
        {
            <div class="groove-empty-card">
                <i class="bi bi-ticket-perforated"></i>
                <h2 class="groove-panel-title">Все още няма <span>покупки</span>.</h2>
            </div>
        }
        else
        {
            <div class="groove-table-card">
                <div class="groove-table-wrap">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Събитие</th>
                                <th>Билет</th>
                                <th>Притежател</th>
                                <th>Цена</th>
                                <th>Купен</th>
                                <th>Статус</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var t in Model.Recent)
                            {
                                var statusClass = t.IsUsed ? "groove-status-badge-muted" : "groove-status-badge-success";
                                var statusText = t.IsUsed ? "Използван" : "Валиден";

                                <tr>
                                    <td><strong>@t.EventTitle</strong></td>
                                    <td>@t.TicketName</td>
                                    <td>@t.OwnerUserName</td>
                                    <td>@t.Price.ToString("0.00") лв.</td>
                                    <td>@t.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                                    <td><span class="groove-status-badge @statusClass">@statusText</span></td>
                                    <td>
                                        <a asp-controller="Tickets" asp-action="Details" asp-route-id="@t.Id" class="groove-button groove-button-paper">
                                            <i class="bi bi-eye"></i> Детайли
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    </section>
</section>
